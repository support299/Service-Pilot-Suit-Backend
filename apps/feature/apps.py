from django.apps import AppConfig


class FeatureConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.feature"
    verbose_name = "Success Center — Feature Center"
