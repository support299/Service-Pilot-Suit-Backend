# Tuned for a small EC2 (≈1GB RAM). Drop into /etc/redis/redis.conf.d/ or
# append via setup-celery.sh. Redis only listens locally for Celery + Django cache.
maxmemory 96mb
maxmemory-policy allkeys-lru
save ""
appendonly no
tcp-keepalive 60
